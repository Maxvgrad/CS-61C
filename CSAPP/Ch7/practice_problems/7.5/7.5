A. Main routing is defined in every C program, becase main is defined in loader's startup script.
B. I assume, main routine doesn't need to return anything becuse main is the first program's routing,
so loader's startup script doesn't rely on return value...
